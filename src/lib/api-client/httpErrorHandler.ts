/**
 * Handle Http errors
 * @param errorCode
 * @returns
 */
const handleError = (errorCode: number) => {
  if (errorCode === 401) {
    //do things
  }
  return null;
};

export default handleError;
